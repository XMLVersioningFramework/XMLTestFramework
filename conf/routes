# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.Application.index()
GET     /getUserStories				controllers.Application.getUserStories()
GET     /getUserStory/:userStoryName				controllers.Application.getUserStory(userStoryName: String)
#GET     /addDataPoint/:id           controllers.Application.addDataPoint(id: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /webjars/*file              controllers.WebJarAssets.at(file)
